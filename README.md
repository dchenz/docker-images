# Source code for docker hub images

Build and tag an image using the git short hash. Each project contains a `spec.json` to minimise the amount of manual typing.

```sh
python3 builder.py PROJECT [--push]
```
